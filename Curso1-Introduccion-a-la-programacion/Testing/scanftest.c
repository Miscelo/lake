#include <stdio.h>


int main(){
	int n;
	double distancia;
	char nombre[20];
	scanf("%d%lf%s",&n, &distancia, &nombre);
	printf("Das sind die argumente: %i, %d, %s",n,distancia,nombre);


	return 0;
}
